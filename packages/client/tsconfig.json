{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true,
    "types": [],
    // DOM lib for fetch/WebSocket/Blob TYPES. The connection never touches
    // window/document; transports are injectable for tests and node.
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src", "test"]
}
