{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true,
    "types": [],
    // DOM lib is for TYPE references at contract boundaries (HTMLElement, Blob)
    // only. Core code must never touch DOM/window at runtime.
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src", "test"]
}
